{
  "schemaVersion": 2,
  "id": "claw-kit",
  "name": "claw-kit",
  "description": "Cindy workflow tools, lifecycle guidance, and Skills for claw-kit.",
  "whenToUse": "Use for Cindy sessions that need claw-kit plan, task, subplan, or workflow operations.",
  "command": "claw-kit",
  "version": "0.2.23.0",
  "kind": "chip",
  "entry": "main.js",
  "launch": "resident",
  "icon": "assets/icon.png",
  "settingsHtml": "panel.html",
  "slots": ["agent", "node", "session-context", "skill", "subscribe", "tool", "card"],
  "agent": { "background": true },
  "node": {
    "entry": "node/claw-worker.cjs",
    "protocol": "json-rpc-stdio",
    "lifecycle": "resident"
  },
  "subscribe": {
    "topics": ["session", "turn"]
  },
  "tools": [
    {
      "name": "list_tools",
      "description": "List the available claw-kit workflow operations. Omit category for an overview, or pass a category to see operation names, descriptions, and argument schemas.",
      "parameters": {
        "type": "object",
        "properties": {
          "category": {
            "type": "string",
            "description": "Optional operation category returned by a previous overview."
          }
        }
      }
    },
    {
      "name": "call_tool",
      "description": "Run one claw-kit workflow operation in the current Cindy session. Use a name returned by list_tools and pass only that operation''s JSON arguments; Cindy supplies the session and workspace identity.",
      "parameters": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Operation name returned by list_tools."
          },
          "args": {
            "type": "object",
            "description": "Arguments for the selected operation."
          }
        },
        "required": ["name"]
      }
    }
  ],
  "skill": {
    "items": [
      {
        "dir": "skills/using-claw-kit",
        "name": "using-claw-kit",
        "description": "Use first whenever the claw-kit Cindy plugin is enabled or its start prompt is present; check your available tools and follow the matching execution route."
      },
      {
        "dir": "skills/planning",
        "name": "planning",
        "description": "Use when a planning step needs to refine a user request into clear requirements, scope, completion criteria, dependencies, and executable tasks, especially when task boundaries or an evidence-dependent route still need judgment."
      },
      {
        "dir": "skills/researcher",
        "name": "researcher",
        "description": "Use for complex research questions that require an independent, multi-step process of gathering and synthesizing evidence—not direct fact lookups or routine searches."
      },
      {
        "dir": "skills/claw-kit-doc",
        "name": "claw-kit-doc",
        "description": "Use when a user needs claw-kit documentation for updates, project configuration, or Truth and ADR formats."
      }
    ]
  }
}
